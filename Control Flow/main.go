package main

import (
	// "bufio"
	// "bufio"
	"fmt"
	// "os"
	// "strconv"
	// "strings"
	// "os"
	// "strings"
)

func main() {
	// fmt.Println("Hello World!")

	// message := "hello"
	// num := 42
	// num = 13

	// fmt.Println(message, num)

	// reader := bufio.NewReader(os.Stdin)
	// line, err := reader.ReadString('\n')

	// if err != nil {
	// 	panic(err)
	// }

	// answer := strings.TrimSpace(line) == "Joy"

	// fmt.Println(answer)

	// message := "hello mr. smith"

	// answer := strings.Contains(message, "hello Mr")

	// fmt.Println(answer)

	// reader := bufio.NewReader(os.Stdin)
	// line, err := reader.ReadString('\n')

	// if err != nil {
	// 	panic(err)
	// }
	// // if the user provides Mr, then print "Hello Sir"
	// // if the user provides Ms, then print "Hello Madam"

	// message := strings.TrimSpace(line)

	// if message == "Mr." {
	// 	fmt.Println("Hello Sir")
	// }

	// if message == "Ms." {
	// 	fmt.Println("Hello Madam!")
	// }

	// reader := bufio.NewReader(os.Stdin)
	// line, err := reader.ReadString('\n')

	// if err != nil {
	// 	panic(err)
	// }

	// num, err := strconv.Atoi(strings.TrimSpace(line))

	// fmt.Println(num)

	// if num <= 10 {
	// 	fmt.Println("The number is very small")
	// } else if num > 10 {
	// 	fmt.Println("The number is small")
	// } else if num > 100 {
	// 	fmt.Println("The number is very medium")
	// } else if num > 500 {
	// 	fmt.Println("The number is very small")
	// }

	// reader := bufio.NewReader(os.Stdin)
	// line, err := reader.ReadString('\n')

	// if err != nil {
	// 	panic(err)
	// }

	// animalType := strings.TrimSpace(line)

	// if animalType == "Mammal" {
	// 	fmt.Println("An example is a hooman")
	// } else if animalType == "Amphibian" {
	// 	fmt.Println("An example is a frog")
	// } else if animalType == "Birds" {
	// 	fmt.Println("An example is the eagle")
	// } else if animalType == "Fish" {
	// 	fmt.Println("An example is the Salmon")
	// } else {
	// 	fmt.Println("I don't know the animal type")
	// }

	// reader := bufio.NewReader(os.Stdin)
	// line, err := reader.ReadString('\n')

	// if err != nil {
	// 	panic(err)
	// }

	// daysOfWeek := strings.TrimSpace(line)

	// switch daysOfWeek {
	// case "Monday":
	// 	fallthrough
	// case "Tuesday":
	// 	fallthrough
	// case "Wednesday":
	// 	fallthrough
	// case "Thurday":
	// 	fallthrough
	// case "Friday":
	// 	fmt.Println("It's a workday")
	// default:
	// 	fmt.Println("It's a weekday")
	// }

	// pointX, pointY := 6, 3
	// topLeftX, topLeftY := 1, 5
	// botRightX, botRightY := 5, 1

	// betweenLeftAndRight := pointX > topLeftX && pointX < botRightX
	// betweenBotAndTop := pointY > botRightY && pointY < topLeftY

	// if betweenLeftAndRight && betweenBotAndTop {
	// 	fmt.Println("The point is inside the rectangle")
	// } else {
	// 	fmt.Println("The point is outside the rectangle")
	// }

	num := 1

	for num <= 10 {
		fmt.Println(num)
		num++
	}
}
